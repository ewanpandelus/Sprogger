using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Rotation : MonoBehaviour
{
  
    /*
    public void RotateTowardsChild(GameObject objToRotateTo)
    {
        if (objToRotateTo)
        {
            Vector3 dir = objToRotateTo.transform.position - transform.position;
            transform.rotation = Quaternion.FromToRotation(Vector3.forward, dir);
            transform.LookAt(objToRotateTo.transform);
        }
    }

    */
 
}
